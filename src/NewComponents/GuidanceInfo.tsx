import React from 'react';
import { View, Text } from 'react-native';
import styles from './GuidanceInfo.style';

type Props = {
};

const GuidanceInfo = ({ }: Props) => {
    return (
        <View>
            <Text>GuidanceInfo</Text>
        </View>
    );
};